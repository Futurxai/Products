# shared/

Small, dumb, reusable presentation components with no knowledge of the domain — `button/`, `star-rating/`, `badge/`. If a component needs an injected facade or knows what a "puzzle experience" is, it belongs in `features/`, not here.

Added incrementally as `features/` components need them, starting Milestone M3.
