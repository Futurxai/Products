{
  "name": "@futurx/puzzle-module-functions",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud Functions for the Puzzle Module — deployed as the 'puzzle-module' codebase into the shared lovedigitally-app Firebase project (see ../firebase.json).",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions --project lovedigitally-app",
    "pretest": "npm run build",
    "test": "jasmine --config=jasmine.json",
    "lint": "eslint --ext .ts src"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.1"
  },
  "devDependencies": {
    "@types/jasmine": "^5.1.4",
    "@types/node": "^20.14.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.0",
    "firebase-functions-test": "^3.3.0",
    "jasmine": "^5.2.0",
    "typescript": "~5.5.4"
  }
}
